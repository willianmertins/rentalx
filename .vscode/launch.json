{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      // "protocol": "inspector",
      "name": "Launch Program",
      "port": 9229,
      // "address": "localhost",
      "remoteRoot": "/usr/app/",
      "sourceMapPathOverrides": {
        "/app/*": "${workspaceRoot}/*"
      },
      "skipFiles": ["<node_internals>/**"],
      "outFiles": ["${workspaceFolder}/**/*.js"]
    }
  ]
}
